{
    "name": "console2",
    "private": true,
    "scripts": {
        "compile": "yarn workspaces run compile",
        "build": "yarn workspaces run build",
        "start": "yarn compile && yarn workspace @console2/console start"
    },
    "workspaces": [
        "packages/*"
    ]
}
